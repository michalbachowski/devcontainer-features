{
    "name": "Snyk (via curl)",
    "id": "snyk-cli",
    "version": "0.0.1",
    "description": "Installs snyk-cli by downloading artifact from GitHub",
    "options": {
        "version": {
            "default": "v1.1294.0",
            "description": "Select the version to install.",
            "type": "string"
        }
    }
}