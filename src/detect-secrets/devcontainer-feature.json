{
    "name": "detect-secrets (via pipx)",
    "id": "detect-secrets",
    "version": "0.0.2",
    "description": "[detect-secrets](https://github.com/Yelp/detect-secrets) is module for detecting secrets within a code base.",
    "options": {
        "version": {
            "default": "1.5.0",
            "description": "Select the version to install.",
            "type": "string"
        }
    }
}
