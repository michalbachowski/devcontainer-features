{
    "name": "Allow to load any local file during shell init",
    "id": "shell-local",
    "version": "0.0.1",
    "description": "Adds a step to load (source) custom shell scrip when initializing Bash or Zsh.",
    "options": {
        "bash_script_path": {
            "default": "",
            "description": "Path to a script to be loaded when initializing Bash.",
            "type": "string"
        },
        "zsh_script_path": {
            "default": "",
            "description": "Path to a script to be loaded when initializing Zsh.",
            "type": "string"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils",
        "ghcr.io/michalbachowski/devcontainer-features/bash-sensible",
        "ghcr.io/stuartleeks/dev-container-features/shell-history"
    ]
}
