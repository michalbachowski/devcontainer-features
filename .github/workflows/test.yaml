name: "CI - Test Features"
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  tst-auto:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        features:
          - bash-prompt
          - bash-sensible
          - custom-ca-cert
          - sbt-cache
          - scalafmt-native
          - shell-local
          - snyk-cli
          - zsh-prompt-currenttime
        baseImage:
          - bachowskimichal/devcontainer-test-ubuntu:v2
          - bachowskimichal/devcontainer-test-debian:v2
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' against '${{ matrix.baseImage }}'"
        run: devcontainer features test --skip-scenarios --skip-duplicated --remote-user root -f ${{ matrix.features }} -i ${{ matrix.baseImage }} .

  tst-dup:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        features:
          - bash-prompt
          - bash-sensible
          - custom-ca-cert
          - sbt-cache
          - scalafmt-native
          - shell-local
          - snyk-cli
          - zsh-prompt-currenttime
        baseImage:
          - bachowskimichal/devcontainer-test-ubuntu:v2
          - bachowskimichal/devcontainer-test-debian:v2
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' against '${{ matrix.baseImage }}'"
        run: devcontainer features test --skip-scenarios --skip-autogenerated --remote-user root -f ${{ matrix.features }} -i ${{ matrix.baseImage }} .

  tst-scn:
    runs-on: ubuntu-latest
    continue-on-error: true
    strategy:
      matrix:
        features:
          - bash-prompt
          - bash-sensible
          - custom-ca-cert
          - pre-commit-initialize
          - sbt-cache
          - scalafmt-native
          - shell-local
          - snyk-cli
          - zsh-prompt-currenttime
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Generating tests for '${{ matrix.features }}' scenarios"
        run: devcontainer features test -f ${{ matrix.features }} --skip-autogenerated --skip-duplicated .

  test-global:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: "Install latest devcontainer CLI"
        run: npm install -g @devcontainers/cli

      - name: "Testing global scenarios"
        run: devcontainer features test --global-scenarios-only --remote-user root -i bachowskimichal/devcontainer-test-ubuntu:v2 .
